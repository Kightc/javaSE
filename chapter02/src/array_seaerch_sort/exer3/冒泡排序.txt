冒泡排序：
    从数组的第一个元素开始，不断地和它后面相邻的元素进行比较（即第一个元素和第二个元素比较，第二个元素和第三个元素比较
    第三个元素和第四个元素比较…………）如果前面的元素比它后面相邻的元素大的话，那么这两个元素互换位置，直到最大的一个元素
    被排到了数组的最后的位置，此后再从第一个元素进行第二轮的排序，直到数组中的元素排序完成。

案例1：使用冒泡排序，实现整型数组得排序操作

例如：int[] arr = new int[]{34, 54, 3, 2, 65, 7, 34, 5, 76, 34, 67}