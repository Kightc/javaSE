
一、类中属性(当前只考虑实例变量)赋值过程
    1.在类的属性中，可以有哪些位置给属性赋值？
        ①默认初始化(赋值)
        ②显示初始化(赋值)
        ③构造器中初始化(赋值)
        **********************************
        ④通过“对象.方法”的方式赋值
        ⑤通过“对象.属性"的方式赋值(前提是对应的属性声明的不是private)

    2.这些位置执行的先后顺序是怎样？
        ① - ② - ③ - ④/⑤

    3.以上操作在对象创建的过程中可以执行的次数如何
        ①、②、③只能执行一次
        ④、⑤可以执行多次

二、JavaBean的理解




三、读懂UML类图